# =============================================================================
# Kianax Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.
# All apps in the monorepo load from this single file.
#
# For Convex server functions, you must ALSO set these in the Convex dashboard:
#   npx convex env set GOOGLE_CLIENT_ID <value>
#   npx convex env set GOOGLE_CLIENT_SECRET <value>
#   npx convex env set SITE_URL <value>
# =============================================================================

# -----------------------------------------------------------------------------
# Convex (Required)
# -----------------------------------------------------------------------------
# Get these from: npx convex dev (auto-generated)

CONVEX_URL=https://your-deployment.convex.cloud
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Used by Convex auth - set to your deployment's HTTP Actions URL
CONVEX_SITE_URL=https://your-deployment.convex.site

# -----------------------------------------------------------------------------
# Temporal (Required for workflow execution)
# -----------------------------------------------------------------------------
# Local development (default)
TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_NAMESPACE=default

# Temporal Cloud (production) - uncomment and fill in
# TEMPORAL_ADDRESS=your-namespace.tmprl.cloud:7233
# TEMPORAL_NAMESPACE=your-namespace
# TEMPORAL_CLIENT_CERT=<base64-encoded-cert>
# TEMPORAL_CLIENT_KEY=<base64-encoded-key>

# Task queue for workers (optional, defaults to "default")
# TASK_QUEUE=default

# -----------------------------------------------------------------------------
# Google OAuth (Required for authentication and Google integrations)
# -----------------------------------------------------------------------------
# Create at: https://console.cloud.google.com/apis/credentials
# Required scopes: openid, email, profile
# For Google Calendar/Sheets plugins, add respective scopes

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# -----------------------------------------------------------------------------
# Site URL (Required)
# -----------------------------------------------------------------------------
# Your app's public URL - used for OAuth redirects

SITE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# OpenAI (Optional - for AI chat features)
# -----------------------------------------------------------------------------
# Get from: https://platform.openai.com/api-keys

# OPENAI_API_KEY=sk-...
