# =============================================================================
# Kianax Environment Variables - Reference
# =============================================================================
# Each app has its own .env.local file. See:
#   - apps/web/.env.example
#   - apps/workers/.env.example
#   - apps/server (uses Convex dashboard for env vars)
#
# This file is for reference only.
# =============================================================================

# --- Web App (apps/web/.env.local) ---
# NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
# TEMPORAL_ADDRESS=localhost:7233
# TEMPORAL_NAMESPACE=default
# SITE_URL=http://localhost:3000
# OPENAI_API_KEY=sk-...  (optional)

# --- Workers (apps/workers/.env.local) ---
# CONVEX_URL=https://your-deployment.convex.cloud
# TEMPORAL_ADDRESS=localhost:7233
# TEMPORAL_NAMESPACE=default
# TASK_QUEUE=default  (optional)

# --- Convex Server (set via dashboard or CLI) ---
# npx convex env set GOOGLE_CLIENT_ID <value>
# npx convex env set GOOGLE_CLIENT_SECRET <value>
# npx convex env set SITE_URL <value>
# npx convex env set CONVEX_SITE_URL <value>
